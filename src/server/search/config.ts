// Search layer configuration (server-only)

export const SEARCH_MAX_AGE_DAYS = 30;

// MVP: keep page size fixed in code (not URL-controlled).
// Note: current UI has no pagination; callers may omit paging.
export const DEFAULT_PAGE_SIZE = 20;
